<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.camerascanner">

    <!-- Quyền camera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Quyền cho Android 12 trở xuống (API <= 32) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Quyền cho Android 13+ (API >= 33) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Camera là tính năng không bắt buộc -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CameraScanner">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CameraActivity" android:exported="false" />

        <activity android:name=".ImagePreviewActivity" android:exported="false" />

        <activity android:name=".CropActivity" android:exported="false"/>

        <activity android:name=".PdfGenerationAndPreviewActivity" android:exported="false"/>
    </application>

</manifest>
